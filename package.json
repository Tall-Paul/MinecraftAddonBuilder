{
  "name": "minecraft-addon-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Search, download, and install Minecraft Bedrock addons into Docker-hosted servers",
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "start": "npm run start -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
